# Reward App Backend

Express and MongoDB API backend for the reward app.

## Setup

```bash
npm install
cp .env.example .env
npm run dev
```

The API runs on `http://localhost:5000` by default.

## Routes

- `GET /health` checks server and database status.
# Reward-app
